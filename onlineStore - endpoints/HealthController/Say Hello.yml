info:
  name: Say Hello
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:9090/test/hello
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
